# Copia este archivo a `keystore.properties` y rellena tus valores reales.
# `keystore.properties` y *.jks están en .gitignore: NUNCA se versionan.
storeFile=../cogela-suave-release.jks
storePassword=CAMBIA_ESTO
keyAlias=cogela-suave
keyPassword=CAMBIA_ESTO
